import { circularArrayLoop } from "../../TwoPointers/circulararrayLoop";

describe("Circular Array Loop", () => {
    test("Testcase 1", () => {
        expect(circularArrayLoop([3, 1, 2])).toBe(true);
    });
});
