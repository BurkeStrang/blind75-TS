import { maxArea } from "../../TwoPointers/containerwithmostwater";

describe("maxArea", () => {
    test("Example test case 1", () => {
        expect(maxArea([1, 8, 6, 2, 5, 4, 8, 3, 7])).toBe(49);
    });
});
