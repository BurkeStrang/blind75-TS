import { isPalindrome } from "../../TwoPointers/validpalidrome";

describe('Valid Palindrome', () => {
  test('Example 1', () => {
    expect(isPalindrome("race a car")).toStrictEqual(false)
  })
});
