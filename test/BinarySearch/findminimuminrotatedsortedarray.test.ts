import { findMin } from "../../BinarySearch/findminimuminrotatedsortedarray";

describe("find minimum in rotated sorted array", () => {
  test("Example 1", () => {
    expect(findMin([3, 4, 5, 1, 2])).toBe(1)
  })
});
