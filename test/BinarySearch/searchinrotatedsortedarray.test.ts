import { search } from "../../BinarySearch/searchinrotatedsortedarray";

describe("search in rotated sorted array", () => {
  test("Example 1", () => {
    expect(search([4, 5, 6, 7, 0, 1, 2], 0)).toBe(4)
  })
});
