import { maxProfit } from "../../SlidingWindow/besttimetobuyandsellstock";

describe("maxProfit", () => {
    test("Example test case 1", () => {
        expect(maxProfit([7, 1, 5, 3, 6, 4])).toBe(5);
    });
});
